<%= simple_form_for([@contract, @contract.feedbacks.build]) do |f| %>
  <div id="rating-form">
     <%= f.input :rating %>
  </div>
  <%= f.input :comment %>
  <%= f.button :submit, class: "btn-custom2" %>

<% end %>