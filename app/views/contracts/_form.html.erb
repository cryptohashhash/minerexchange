<%= simple_form_for @contract do |f| %>
         <%= select_tag(:category_id, options_for_select(@categories), prompt: "Select Category") %>
  <%= f.input :title, label: "Contract Title" %>
  <%= f.input :description %>
  <%= f.input :owner %>
  <%= f.input :price %>
  <%= f.button :submit %>
<% end %>